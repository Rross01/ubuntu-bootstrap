# set -a appends values to existing settings
# server options (set-option -s),
# global session options (set-option -g),
# session options (set-option),
# global window options (set-window-option -g),
# window options (set-window-option)?

set -sg escape-time 10
set -g focus-events on

# History lenght limit
set -g history-limit 10000

# Automatic renumber windows
set -g renumber-windows on

set -g default-terminal "xterm-256color"
set -g terminal-features 'xterm-256color:RGB'

# Start Window Numbering at 1
set -g base-index 1

# Timeout after bind
set -g repeat-time 500

# Change prefix key to C-a
unbind C-b
set -g prefix C-a

# Enable mouse support
set -g mouse on

# Do not detach when destroy session
set -g detach-on-destroy off

# Copy settings
set -g mode-keys vi
set -s copy-command 'wl-copy'
bind -T copy-mode-vi 'v'   send -X rectangle-off \; send -X begin-selection
bind -T copy-mode-vi 'C-v' send -X rectangle-on \; send -X begin-selection
bind -T copy-mode-vi 'y'   send -X copy-selection-and-cancel
bind -T copy-mode-vi MouseDragEnd1Pane send -X copy-pipe-and-cancel
bind -T copy-mode-vi Escape {
    if-shell -F "#{selection_present}" {
        # true
        send-keys -X clear-selection
    } {
        # false
        send-keys -X cancel
    }
}

# new window and retain cwd
bind c new-window -c "#{pane_current_path}"

# paste yanked buffer
bind P paste-buffer

# Split panes
bind - split-window -h -c "#{pane_current_path}"
bind _ split-window -v -c "#{pane_current_path}"

# Screen compatibility
bind a send-prefix

# Reload config
bind r source-file "~/.config/tmux/tmux.conf"

# Select pane and windows
bind -r C-h previous-window
bind -r C-l next-window

# pane navigation
bind -r h select-pane -L  # move left
bind -r j select-pane -D  # move down
bind -r k select-pane -U  # move up
bind -r l select-pane -R  # move right

# Move marked window/pane
bind P move-pane

# Session wizard
bind -n C-f display-popup -w 80 -h 15 -E sh -c "~/.scripts/tmux_session_wizard.sh"

# APPEARENCE AND STATUS BAR
border_active="#539bf5"
border_inactive="#545d68"
bar_text_fg="#909dab"
bar_status_inactive_fg="#545d68"

# panel separator color
set -g pane-border-style "fg=$border_inactive"
set -g pane-active-border-style "fg=$border_inactive"

# general status bar settings
set -g status-style "fg=$bar_text_fg"

# Left status
set -g status-left-length "100"
set -g status-left-style "fg=$bar_text_fg"

# Window status
set -g window-status-style "fg=$bar_status_inactive_fg"
set -g window-status-current-style "fg=$bar_text_fg"

# Right status
set -g status-right ""
