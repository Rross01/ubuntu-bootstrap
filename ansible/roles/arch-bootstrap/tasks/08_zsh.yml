---
- name: Clone zsh repo
  git:
    repo: https://github.com/ohmyzsh/ohmyzsh/
    dest: "{{ zsh_home }}"
    force: true

- name: Change default shell to zsh
  become: true
  user:
    name: "{{ user }}"
    shell: /bin/zsh
